seed: 42

preprocess:
  drop_invalid: true

feature_eng:
  test_ratio: 0.2
  min_user_interactions: 2
  seed: 42

train:
  model_name: mlp
  embedding_dim: 32
  hidden_dim: 64
  batch_size: 256
  max_epochs: 12
  early_stopping_patience: 4
  learning_rate: 0.001
  val_ratio: 0.1
  neg_per_pos: 1
  seed: 42

evaluate:
  k: 10
