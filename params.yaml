seed: 42

preprocess:
  drop_invalid: true

feature_eng:
  test_ratio: 0.2
  min_user_interactions: 2
  seed: 42

train:
  model_name: mlp
  embedding_dim: 32
  batch_size: 256
  max_epochs: 20
  seed: 42

evaluate:
  k: 10
